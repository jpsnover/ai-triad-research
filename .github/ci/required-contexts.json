{
  "_comment": [
    "t/3646 — SSOT MIRROR of main's required status checks. This file is a DESCRIPTION, not",
    "the source of truth. GitHub branch protection is AUTHORITATIVE:",
    "  gh api repos/jpsnover/ai-triad-research/branches/main/protection/required_status_checks",
    "Hand-editing this file changes NOTHING real — it only changes what workflow-lint enforces.",
    "operations/devops/Test-RequiredContextsListDrift.ps1 validates this list against the live",
    "API; that drift-guard MUST be green on main BEFORE the workflow-lint required-context rule",
    "is promoted from warn-only to blocking (a lint enforcing a stale mirror is worse than none).",
    "'workflow: null' = a required context that is GitHub-managed (no repo workflow file to lint),",
    "e.g. CodeQL — it is tracked here for drift parity but has no lintable workflow."
  ],
  "required_contexts": [
    { "context": "ci-gate", "workflow": ".github/workflows/ci.yml" },
    { "context": "joint-gv-guard", "workflow": ".github/workflows/joint-gv-guard.yml" },
    { "context": "CodeQL", "workflow": null },
    { "context": "livefire-broken", "workflow": ".github/workflows/livefire-broken.yml" }
  ]
}
