**Date:** 2026-07-26
**Working on:** Heavy batch failure-pattern intake from fleet pings (spanned 2026-07-17 → 07-26). New patterns #69–#78 + many 2nd/3rd instances of existing patterns; all committed by pathspec, INDEX reconciled each time. INDEX now Total 79 / Resolved 21 / Active 58. Latest (07-26): t/1726 strict-mode unguarded `.factual_claims` access (instance of Strict Mode Eval Failures; driver = LLM-omitted optional fields; TL: not hookable, a3c0f77f); 6th cp1252 UnicodeEncodeError (`python -c` print-to-stdout, 1f635d29, self-correcting); #79 cp-from-shared-tree sweeps uncommitted WIP into a worktree commit (7th worktree-land cluster hazard, /land-from-worktree step-3 refinement, a5493fa1, flagged to TL batch p/8#91).
**Status:** Complete for now. Biggest theme = WORKTREE-LAND/DIVERGENCE cluster (own consolidated INDEX Quick-Ref entry): 5 git footguns #72/#74/#75/#76 + #73 facet-B, plus env hazard #77 (npm ci empty pkg dir). #76 = `git commit -- <list>` silently omits a glob-staged file → broken origin (file-count is the tell). Also: #69 post-compaction, #70 vitest mock-harness, #71 smoke boolean, #73 Windows-Git-Bash chains, #74 bare-restore-wrong-ref, #75 checkout-- . wrong-scope; instances added to dev-env-tools (jq), Bash-vs-PS (file-size), object-level-forensics (2nd config-failure).
**Key context:** ESCALATION STATUS (2026-07-17): (1) #73 facet A (3 instances) → ACCEPTED by TL into the root "Search Tooling Rule" (p/8#86); not hookable. (2) Worktree cluster #74/#75/#76/#77/#78 + #73 facet-B → in TL's AGENTS.md/`/land-from-worktree` batch, owner-gated, surfaced to owner now (p/8#86); #78 (step-8 `git worktree remove --force`) flagged to fold in before it goes (p/8#87). (3) #76 mechanical defense → Diagnostics SHIPPED the `staged-files-after-commit` hook (operations/diagnostics/check-staged-after-commit.cjs, p/9#33; inert until next sync per #68). (4) STILL OPEN: broad-scope-revert hook #75 → Diagnostics' feasibility call (p/9#27). (5) object-level-forensics recurred as 2nd CONFIG instance despite Resolved root rule (p/9#30) — 3rd → push point-of-use trigger.
**Next:** Watch (a) the owner's go/no-go on TL's now-multi-item batch (worktree cluster incl. #78 step-8 --force, root Search Tooling Rule for grep-&& facet A, Git-Forensics MSYS signature); (b) Diagnostics' #75 broad-scope-revert hook call; (c) verify #76 `staged-files-after-commit` hook LIVE post-sync via manifest (not counters, #68); (d) DevOps' fleet sync sweep — flagged the LARGE-divergence push failure (Diagnostics 46-ahead/52-ahead, p/9#36) as a push-cadence-ceiling breach (p/26#16), merge routed to TL. Escalation thresholds now hot: 3rd bare-commit-sweep → extend pathspec hook; 3rd config-forensics → point-of-use trigger; grep-&& facet A (3x) accepted into root rule. Also added: `pwsh -File` over inline -Command (p/20#23, prevention #7 on the heredoc pattern). Watch #69/#66 2nd instances. Lossy-Error-Boundaries genus closed.
