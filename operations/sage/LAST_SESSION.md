**Date:** 2026-07-17
**Working on:** p/8#73 from TL — recorded the **5th instance** of the [API] Lossy Error Boundaries genus (t/1626, Diagnostics flight-recorder triage of debate a7ddc788). `parseAIJson` silently returned null on a valid 6728-char body → all 7 debater claim sketches dropped (0-of-7, an_nodes:0). Fits the genus (generic recovery masking a real payload); **broadens** it past the provider edge to any recovery/parse boundary that discards a recoverable payload.
**Status:** Complete — dual-write done (api.md + LessonsLearned.md: instance added, Status → "escalation triggered, 5 instances / 3 agents", new prevention #6 = fall-back-to-recoverable-payload + head+tail+truncation-flag capture). INDEX Quick-Reference line updated (counts UNCHANGED — instance, not new pattern; Total stays 69/21/48, API stays 3/0/3). Committed by pathspec.
**Key context:** The t/1626 site (`lib/debate/helpers.ts` parseAIJson / `argumentNetwork.ts`) sits **OUTSIDE the t/1623 hook's current path scope** (keys.ts / providers/* / Test-AIApiKey / Invoke-AI*) — the 5th instance exposes that coverage gap. Escalation action = recommend Diagnostics **expand the t/1623 hook paths** to cover debate JSON-recovery sites, rather than a new prose rule (ADR-001 already mandates ActionableError). t/1623 hook still not verified live — check manifest presence + live-fire, NOT `has_run` (pattern #68).
**Next:** Confirm Diagnostics + TL escalation pings landed and track responses. Watch t/1623 hook going live (via manifest, not counters). Watch for a 2nd instance of #67 (Bash-tool/pwsh) or #66 (stacked-branch landing).
