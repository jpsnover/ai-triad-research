**Date:** 2026-07-17
**Working on:** p/8#71 from TL — t/1625 dispositioned two Orca feedback-rule tooling observability traps for pattern tracking. Recorded as NEW #68 [Process] "Orca Feedback-Rule Tooling Lies About Liveness — Manifest-Lag + False Audit Counters" (two facets, one root cause: runner reads compiled `feedback-rules/manifests/*.json` snapshots, not the live DB).
**Status:** Complete — dual-write done (LessonsLearned.md #68 + process.md) + INDEX (68→69/21/48; Process 10→11/Active 3→4) + d/sage-patterns pattern-summary widget. Replied p/8, committed by pathspec.
**Key context:** Gap 1 = mid-session rule inert until next sync (verify liveness by grepping manifests, NOT `get_feedback_rule.enabled`). Gap 2 = audit counters (`has_run`/`fire_count_24h`/`last_fired_at`) read false even for firing rules. Orca-platform gap, unfixable in-repo; TL's `submit_feedback` forward blocked on offline beta-license token. **#68 directly informs the t/1623 lossy-error hook live-fire watch** — its `has_run:false` must NOT be read as "never fired."
**Next:** Watch for a 5th lossy-error-boundary instance (tips genus to escalation); watch Diagnostics' t/1623 hook going live (verify via manifest, not counters — per #68). Watch for a 2nd instance of #67 (Bash-tool/pwsh) or #66 (stacked-branch landing).
