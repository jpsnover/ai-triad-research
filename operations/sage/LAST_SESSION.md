**Date:** 2026-07-28
**Working on:** Heavy batch failure-pattern intake from fleet pings (spanned 2026-07-17 → 07-26). New patterns #69–#92 + many 2nd/3rd instances; all committed by pathspec, INDEX reconciled each time. INDEX now Total 93 / Resolved 21 / Active 72. #92 [Data] file-type discrimination by presence-of-a-key admits look-alikes → gate on the CONTRACT (nodes[].id) not key presence (PowerShell 2 t/1834). REPEAT OFFENDER flagged: `entity_extraction_log.json` sidecar = 3rd shape-surprise (t/1830 char-explode, p/7#47 probes, t/1834 fake-POV) → candidate for an explicit denylist. Latest (t/1829 gate-integrity, ServerAPI+TL): #90 `verify | tail` masks the real exit (pipe returns tail's 0) = silent false-GREEN (bookkeeping-vs-artifact genus, #84 sibling; fix rc=$? or pipefail); #91 flaky lib/debate suite (aiAdapter/persistenceFaults/cliPipeExit, nondeterministic) = false-RED generator, triage WHICH files first, TL routed HIGH stabilization to DebateTool. Also: data-shape pattern hit PRODUCTION (extraction cmdlet char-explodes bare-string aliases 13/37, t/1830, p/7#47) — but in POWERSHELL (Invoke-EntityExtraction, corrected p/7#49), so TS-union strengthening does NOT cover it; PS-side fix = coerce-at-read shared helper + bare-string Pester fixture. Offender #5 defense splits by surface (TS→union types/tsc; PS→shared coerce helper). Recent: #86 win32 task-stop-kills-wrapper-not-children; #87 npm build false-red on interactive license prompt (no TTY, p/124#4); #88 local verify hangs on LIVE AI backend when keys set / keyless CI misses it (p/234#1, t/1824); #89 subagent "completed" ≠ deliverables exist (p/20#27). NEW cross-cutting genus w/ its own INDEX Quick-Ref: **"bookkeeping ≠ artifact"** — a status/lifecycle/exit signal describes the PROCESS not the DELIVERABLE; verify at object level (#69/#80/#84/#86/#89, ≥5 patterns). Latest additions: #82 (rule-not-applied meta-tracker), #83 (commit-by-pathspec file-granular → sweeps foreign hunks in live data repo, qualifies ADR-005 + serialize-batches convention p/7#39/p/8#106), #84 (background-task gate wrapper `&& echo PASS||echo FAIL` launders exit code → false-green, p/195#3), #85 (post-ADR-007 stale barrel-path citations fail SILENTLY, grep-empty not build-break, p/8#106/#107). #69 broadened to any session boundary + peer-already-landed variant (p/7#42).
**Status:** Complete for now. Dominant theme = WORKTREE-LAND/DIVERGENCE cluster (own consolidated INDEX Quick-Ref): 7 git-side footguns (#72/#74/#75/#76/#79/#81 + #73 facet-B) + 2 env/cleanup (#77 npm-ci empty pkg, #78 worktree-remove --force). #81 = /land-from-worktree sync-back leaves files staged → ADR-005 sweep-bait (supply side of the bare-commit-sweep pattern). Plus a HOOK-INFRA thread: #80 feedback hooks silently dead on Windows (`{workspace_root}` empty → crash → exit-1-suppress). Also this session: #69 post-compaction, #70 vitest mock-harness, #71 smoke boolean, #73 Windows-Git-Bash chains; instances on dev-env-tools(jq), Bash-vs-PS(file-size ×2 same agent), object-level-forensics(2nd config), strict-mode(t/1726), cp1252(6th), push-contention(large-divergence), bare-commit-sweep(2nd), git--flag-order(4th overlay), heredoc(pwsh -File).
**Key context — ESCALATION STATUS (2026-07-26):** (1) #73 facet A (3×) ACCEPTED into root Search Tooling Rule (p/8#86). (2) Worktree cluster + #78/#79/#81 + Git-Forensics MSYS signature → TL's owner-gated /land-from-worktree AGENTS.md batch (p/8#86/#87/#91/#92). (3) #80 hook path-crash CLOSED — Diagnostics audit inlined the only two `{workspace_root}` hooks via node -e (p/9#41); RESIDUAL exit-1-suppresses-silently is Orca Support's platform fix (confirmed live: guard fires, runner logs node exit 1 while suppressing). (4) OPEN: broad-scope-revert hook #75 (p/9#27); DevOps fleet sync sweep for the 46/52 divergence cadence-breach (p/26#16, merge→TL). (5) object-level-forensics recurred as 2nd CONFIG instance despite Resolved root rule (p/9#30).
**Next:** Watch (a) owner go/no-go on TL's batch; (b) Diagnostics' #75 hook call; (c) whether Orca Support fixes the exit-1-suppress contract (#80 Part 2); (d) DevOps sync sweep. HOT thresholds → next instance escalates: 3rd bare-commit-sweep (hook extend), 3rd config-forensics (point-of-use trigger), 9th worktree-land hazard. META-PATTERN now recorded as #82 [Process] "Rule-exists-but-not-applied" with a running tracker + Quick-Ref (p/8#94→#96): 3 instances / 3 offenders so far (#81 step-7 anti-pattern; 2nd config-forensics; strict-mode property access). Decision rule (TL p/8#95/#97): TWO triggers, whichever first — (a) any offender's 4th → point-of-use hook for that offender; (b) class-total ~6 → systemic reinforcement. **BOTH TRIGGERS FIRED 2026-07-26 (class-total ≥12); TWO offenders now trip the per-offender trigger:** #4 direct-commit-to-shared-main (≥5, p/8#99/#100, cleanly hookable) + #5 data-shape type-check-not-applied (≥4, CL p/7#36/#38 — I REVERSED my earlier not-in-#82 call on CL's recurrence argument; hookability caveat). Both dispositioned (TL p/8#104→#109): #4 → HOOK (t/1780, In Review; crisp branch==main signal); #5 → RULE-ONLY (t/1810 — correct normalize-at-fetch ≈ violation, so a detector false-reds = dead gate; strengthened via TS union-types + naming variadic fields). GENERAL CRITERION now logged as #82 prevention #3: the hook lever converts an offender ONLY when its violation is a crisp syntactic signal DISTINCT from correct code ("detectable" = distinguishable-from-correct, not just greppable). Sage STANDING ACTION: keep tagging new distinct offenders + both counters; watch t/1780. Also: git `--` flag-order now 7 instances/7 agents (+TaxEditor p/6#22) — still self-correcting/NOT #82 (git enforces), but at 7× the lever = corrective GUIDANCE (the hook should emit `-m "msg" -- <paths>` on a violation; crisp syntactic signal). Diagnostics ACCEPTED (p/9#47) — folding the corrective-guidance emit into the #80 Part-3 fix (emit "flags before --: git commit -m 'msg' -- <paths>" on a real violation). RESOLVED this batch: #80 hook path-crash (inlined) but NEW Part 3 = the inlined guard now exits 1 on ~every call = fleet-visible noise (Sage + PowerShell 2 p/228#1), Diagnostics to make it exit 0 on clean path; CI Gate-Blindness t/1800 FIXED (DevOps 231e0f3e — audit decoupled to own job; new rule: two gates = separate jobs). Watch #69/#66 2nd instances; the 9th worktree-land hazard; Orca Support exit-1-suppress contract (#80 Part 2). Also recorded: dirty-tree-false-witness pattern gained a CHECK-LAYER variant (t/1808, p/8#104 — an ontology referential-integrity check passed against a dirty worktree; #44/#54/#55 class; prevention: checks must name which tree they validated). Lossy-Error-Boundaries genus closed.
