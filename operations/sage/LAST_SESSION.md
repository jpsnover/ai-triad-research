**Date:** 2026-07-17
**Working on:** Batch failure-pattern intake from fleet pings — recorded 5 new patterns + 1 refinement this session (all committed by pathspec, INDEX reconciled each time).
**Status:** Complete. #69 post-compaction summary-vs-object-state (p/7#37, 278b2af0); overlay-git already-tracked refinement (p/7#39, 7bf3aaff); #70 vitest mock-harness re-export→undefined (p/6#21, 54a254c1); #71 post-deploy smoke Overall-boolean false-red (p/26#12, ce9acafa); #72 landing-worktree pre-push friction — verify dirties snapshot + origin advances (p/206#3 + p/70#7, c1948588); #73 Windows Git Bash breaks command chains — grep zero-match exit + MSYS path-conversion (p/26#14, dd846612). INDEX now Total 74 / Resolved 21 / Active 53.
**Key context:** WORKTREE-LAND/DIVERGENCE GIT-FOOTGUN GENUS now has its own consolidated INDEX Quick-Reference entry — 4 hazards this session from the active push cadence during lands: #72 (`git diff HEAD..origin/main` false-flags own files + verify dirties snapshots), #74 (bare `git restore <file>` reverts to local HEAD wiping origin-side content, 0958ff2d), #75 (`git checkout -- .`/`git restore .` reverts ALL unstaged tracked edits; untracked survivors mask the loss — DebateTool t/1686, edd60370), + #73 facet-B signature. Common root: during a land/divergence never assume a file's working-tree content is local HEAD — scope by path, act object-level. Recommended a broad-scope-revert PreToolUse hook to Diagnostics (p/9#27); handed the cluster to TL for the /land-from-worktree batch (p/8#81/#82). Also this session: #69 post-compaction, #70 vitest mock-harness, #71 smoke boolean, overlay-git refinement, jq-shim instance.
**Next:** Watch Diagnostics' feasibility call on the broad-scope-revert hook (p/9#27) and the overlay owner's decision on TL's batched /land-from-worktree rule + Git-Forensics trap line. A 5th cluster hazard → consider escalating to a root AGENTS.md worktree-land rule. #73 facet A (grep -c zero-match `&&`-break) now 2 instances (DevOps + ServerAPI) — watch for a 3rd before a hook. Also watch for a 2nd instance of #69 / #66. Lossy-Error-Boundaries genus fully closed.
