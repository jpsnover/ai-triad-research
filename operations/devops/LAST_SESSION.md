**Date:** 2026-07-20
**Working on:** Lossless ACA image-slim (cut ~37s long-idle cold start by dropping the torch/ML venv). TL ruled the embedding half via **in-process ONNX**, not an API fallback (e/42#3).
**Status:** Coordination complete; DevOps implementation (t/1642) still HELD on Server AI Proxy. No Dockerfile edits made.

**Update 2026-07-20 (ping p/208#3 from Server AI Proxy — manifest FROZEN, one gate left, still HOLD):**
- **Bake manifest is now FROZEN** (supersedes the earlier "in flux" note). Env var **`AI_TRIAD_ONNX_MODEL_DIR`** points at a **FLAT dir** containing exactly three files: **`model.onnx` (fp32 / full-precision), `tokenizer.json`, `tokenizer_config.json`**. **CPU EP.** No HF Hub fetch at runtime — files-present short-circuit; **verify with network blocked.** This replaces the org-prefixed transformers.js layout: Shared Lib hardened the existing **onnxruntime-node** module in place (t/1651 freeze SHA `61260804`) rather than a transformers.js rewrite, so the `env.allowRemoteModels`/`env.localModelPath` transformers.js pins from the old plan no longer apply.
- **Both hosted AI-path SHAs are RESOLVED on origin/main:** embeddings `39490e79` (t/1641 embedding half + t/1643), NLI `41b4b338` (t/1641 NLI half). Both hosted paths now fall back to the shared in-process ONNX all-MiniLM-L6-v2 encoder — no Python venv needed for either.
- **ONE remaining gate before slimming — t/1650:** `/api/nli/classify` still needs ServerAPI's resolved BYOK/free-tier key for the hosted fallback. **Status (t/1650#1): implemented + committed as `ad581afc` on LOCAL main but NOT on origin/main** — blocked on a local-main↔origin-main divergence reconciliation ServerAPI escalated to TL (t/1641 `41b4b338` is on origin but not under local `ad581afc`). Verify gate is red on local main only because the base lacks t/1641 (server.ts:1320 TS2554). Do NOT slim the venv until `ad581afc`'s fix reaches origin/main. Details on t/1641#11; "origin reconcile is TL's."

**TL decision (e/42#3) — Option B, in-process ONNX:**
- Embeddings: run all-MiniLM-L6-v2 in-process at 384-dim via `onnxruntime-node ^1.27.0` + `@huggingface/transformers ^4.2.0` — BOTH already direct deps (taxonomy-editor/package.json L60/L52; verified). Same weights, same vector space, zero corpus migration. Rejects Gemini re-embed (3072-dim, different space = one-way-door data migration → ADR-gated, NOT default).
- NLI: prompt-based entailment fallback via new UsageID `server.nli-classify-fallback`; Server AI Proxy owns, no design gate (ADR-006). Becomes the only hosted NLI path post-slim (accepted quality change).
- Latent bug: existing `computeQueryEmbedding` Gemini fallback already emits wrong-space 3072-dim — masked only because Python still ships. Server AI Proxy to file a tracking ticket ref'd on t/1641.

**DevOps deliverable (my e/42#5):** transformers.js fetches weights from HF Hub at runtime by default — must BAKE the MiniLM ONNX weights into the image and pin `env.allowRemoteModels=false` + `env.localModelPath` so no network fetch hits the cold-start path. Net image cost: ~90MB MiniLM ONNX vs ~700MB torch venv dropped. I add the bake step to Dockerfile as part of t/1642 and verify no runtime fetch.

**Revised t/1642 scope (blocked_by t/1641):** (1) slim `deploy/azure/Dockerfile.base` — remove ML venv (L37-39, L43-45, L49-50, L54-58, chown /opt/ml-env) KEEPING python3 (pty-broker.py needs it); (2) bake MiniLM ONNX model into the app/base image + verify no runtime HF fetch; (3) rebuild+push slim base w/ new date tag; (4) bump `taxonomy-editor/Dockerfile` L64 FROM tag; (5) rebuild app image; verify; deliberate deploy via deploy-azure.yml; measure image-size + cold-start delta. Land from isolated worktree (/land-from-worktree).

**Open coordination items (TL e/42#6, recorded on t/1641#3 as hard ACs):**
- **Offline-load AC:** weights baked + `env.allowRemoteModels=false` + `env.localModelPath`; verify no HF fetch with network blocked.
- **Path contract:** Server AI Proxy proposes the literal `localModelPath` + file manifest on t/1641; DevOps mirrors it byte-for-byte in the t/1642 bake. My e/42#7 proposed base `/app/models/` (aitriad-owned, stable under WORKDIR /app) and flagged that the FULL transformers.js set must bake — `config.json`, `tokenizer.json`, `tokenizer_config.json`, `special_tokens_map.json` + `onnx/model.onnx` — not just the graph, or the tokenizer/config triggers a Hub fetch and hard-fails offline.
- **Equivalence check gates "lossless":** ONNX encode must reproduce stored ST 384-dim vectors within tolerance; run it against whichever graph we ship. Worst case = one-time same-space corpus regen, NOT the rejected Gemini migration.
- **Graph precision (TL e/42#8, recorded t/1641#4): bake FULL-PRECISION by default** (~90MB, likelier to pass the equivalence check; quantized shifts numerics → drift risk). Ship quantized ONLY if it passes the check AND the size delta proves material. DevOps bakes whatever graph Server AI Proxy pins + the check passes on.
- **Path contract is Server AI Proxy's to pin on t/1641 (TL e/42#8):** TL endorsed my `/app/models/` as a "sound default"; SAP confirms/counter-names it and pins the exact file manifest + subdir layout (flat vs `<org>/all-MiniLM-L6-v2/…`). DevOps mirrors byte-for-byte in t/1642. Both actions are SAP's; nothing for DevOps to reply to.

**Next:** Wait for Server AI Proxy ping with committed SHAs for BOTH the ONNX embed swap and the NLI fallback, AND the pinned literal model path/manifest on t/1641. Do NOT touch Dockerfile.base until then. When it lands: `git show HEAD:...aiBackends.ts` to confirm both are in committed code, then execute t/1642 from a worktree — bake model to the pinned path, verify no runtime fetch (network blocked), bump Dockerfile L64 FROM tag, measure image-size + cold-start delta.
