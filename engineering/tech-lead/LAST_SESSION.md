**Date:** 2026-07-09 — t/1439 CodeGraph evaluation (Orca unreachable all session — no MCP ticket/ping/identity tools at all)
**Working on:** Re-scoped t/1439 twice (first wrongly to taxonomy graph data, then correctly to the codebase's own import graph) after owner correction. Verdict: don't adopt CodeGraph itself (LLM-writes-a-script-per-query is worse than a maintained tool); its principle exposed a real one-hop-only gap in `taxonomy-editor/scripts/depgraph.mjs`.
**Status:** Shipped and committed (16574ec8): added `--blast-radius <pattern> [--depth N]`, `--path <fromPattern> <toPattern>`, `--cycles` to depgraph.mjs — all tested live (125-file 2-hop blast radius, 3-hop path, 39 deduped cycles found, mostly harmless type-only). Additive only, existing flags unaffected.
**Key context:** `depgraph.mjs` is Taxonomy Editor's scope, not mine — edited directly on owner's explicit instruction because Orca (ticket routing, ownership resolution, ping/email) is down this whole session. Taxonomy Editor has NOT been notified of this change yet — do that (ping + reference commit 16574ec8) as soon as Orca reconnects, before they touch this file.
**Next:** Ping Taxonomy Editor re: depgraph.mjs changes once Orca is back; check ticket queue (couldn't during this session — no `list_tickets` access).
