{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jsnov/ai-triad-research/taxonomy/schemas/situations-taxonomy.schema.json",
  "title": "Situations Taxonomy File",
  "description": "Schema for the situations taxonomy file in the AI Triad Research project. Situation nodes represent DOLCE D&S Situations — contested concepts where all three POV Descriptions classify the same phenomenon differently.",
  "type": "object",
  "required": [
    "_schema_version",
    "_doc",
    "last_modified",
    "nodes"
  ],
  "additionalProperties": false,
  "properties": {
    "_schema_version": {
      "type": "string",
      "description": "Semantic version of the schema this file conforms to.",
      "examples": ["1.0.0"]
    },
    "_doc": {
      "type": "string",
      "description": "Human-readable documentation string describing this file's purpose and editing conventions."
    },
    "last_modified": {
      "type": "string",
      "format": "date",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "ISO 8601 date (YYYY-MM-DD) of the last modification to this file.",
      "examples": ["2026-02-22"]
    },
    "nodes": {
      "type": "array",
      "description": "Ordered list of situation taxonomy nodes.",
      "items": {
        "$ref": "#/$defs/SituationNode"
      }
    }
  },
  "$defs": {
    "Interpretation": {
      "description": "A POV's interpretation of a situation. Either a plain string (legacy format) or a BDI-decomposed object separating empirical claims, normative commitments, and strategic reasoning within the interpretation.",
      "oneOf": [
        {
          "type": "string",
          "minLength": 1,
          "description": "Legacy format — single paragraph describing the POV's interpretation."
        },
        {
          "type": "object",
          "description": "BDI-decomposed interpretation — separates the POV's empirical claims (belief), normative commitments (desire), and strategic reasoning (intention) for richer analytical downstream use.",
          "required": ["belief", "desire", "intention", "summary"],
          "additionalProperties": false,
          "properties": {
            "belief": {
              "type": "string",
              "minLength": 1,
              "description": "The empirical claim(s) this POV makes about the situation. What factual assertions does this perspective rely on? Test: 'Could this be proven true or false with evidence?'"
            },
            "desire": {
              "type": "string",
              "minLength": 1,
              "description": "The normative commitment(s) this POV holds regarding the situation. What does this perspective argue should happen? Test: 'Is this about what ought to happen?'"
            },
            "intention": {
              "type": "string",
              "minLength": 1,
              "description": "The strategic reasoning this POV applies to the situation. How does this perspective argue the issue should be approached? Test: 'Is this about how to achieve a goal or reason about the issue?'"
            },
            "summary": {
              "type": "string",
              "minLength": 1,
              "description": "1-2 sentence summary of the full interpretation for UI display and backward compatibility. Should capture the essence of all three BDI components."
            }
          }
        }
      ]
    },
    "SituationNode": {
      "type": "object",
      "description": "A situation node representing a DOLCE Situation — a contested concept that all three POV Descriptions classify differently. The interpretations field implements the D&S mechanism: three Descriptions of one Situation.",
      "required": [
        "id",
        "label",
        "description",
        "interpretations",
        "linked_nodes",
        "conflict_ids"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "pattern": "^sit-\\d{3}$",
          "description": "Unique identifier for this situation node. Format: sit-{sequence}.",
          "examples": ["sit-001", "sit-012"]
        },
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "Short human-readable title for this situation.",
          "examples": ["AI Existential Risk", "Regulatory Capture"]
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Genus-differentia description: 'A situation that [differentia]. Encompasses: ... Excludes: ...' — a self-contained paragraph following DOLCE D&S conventions."
        },
        "interpretations": {
          "type": "object",
          "description": "Per-POV interpretations — the DOLCE D&S mechanism. Three Descriptions of one Situation. Each POV explains how it understands this concept. Each interpretation is either a plain string (legacy) or a BDI-decomposed object with belief, desire, intention, and summary fields.",
          "required": ["accelerationist", "safetyist", "skeptic"],
          "additionalProperties": false,
          "properties": {
            "accelerationist": {
              "$ref": "#/$defs/Interpretation",
              "description": "How the accelerationist POV interprets this situation."
            },
            "safetyist": {
              "$ref": "#/$defs/Interpretation",
              "description": "How the safetyist POV interprets this situation."
            },
            "skeptic": {
              "$ref": "#/$defs/Interpretation",
              "description": "How the skeptic POV interprets this situation."
            }
          }
        },
        "linked_nodes": {
          "type": "array",
          "description": "List of POV node IDs that relate to this situation.",
          "items": {
            "type": "string"
          }
        },
        "conflict_ids": {
          "type": "array",
          "description": "List of conflict claim IDs associated with this situation.",
          "items": {
            "type": "string",
            "pattern": "^conflict-[a-z0-9-]+$"
          }
        },
        "disagreement_type": {
          "type": "string",
          "enum": ["definitional", "interpretive", "structural"],
          "description": "Classification of the cross-POV disagreement. definitional: POVs define the concept differently. interpretive: POVs agree on definition but disagree on implications. structural: POVs frame the issue differently at a foundational level."
        },
        "debate_refs": {
          "type": "array",
          "description": "Debate session IDs that referenced this node. Added by harvest.",
          "items": { "type": "string" }
        },
        "graph_attributes": {
          "type": "object",
          "description": "Rich analytical attributes generated by Invoke-AttributeExtraction. Optional — populated organically.",
          "additionalProperties": true,
          "properties": {
            "node_scope": {
              "type": "string",
              "enum": ["claim", "scheme", "bridging"],
              "description": "AIF-aligned classification of the node's role in argumentation."
            },
            "epistemic_type": {
              "type": "string",
              "description": "The epistemic category of the node's content."
            },
            "steelman_vulnerability": {
              "description": "The strongest counterargument(s). String (legacy) or per-POV object.",
              "oneOf": [
                { "type": "string" },
                {
                  "type": "object",
                  "properties": {
                    "from_accelerationist": { "type": "string" },
                    "from_safetyist": { "type": "string" },
                    "from_skeptic": { "type": "string" }
                  },
                  "additionalProperties": false
                }
              ]
            }
          }
        }
      }
    }
  }
}
