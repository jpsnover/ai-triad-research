# ci-gate verify — case (a) docs-only

Throwaway file for t/1962 verify case (a): a docs-only change matches no
path-filter, so all code jobs skip and `ci-gate` must report GREEN.

DO NOT MERGE. This PR and branch are deleted after verification.
