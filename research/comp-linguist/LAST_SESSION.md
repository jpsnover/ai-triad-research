**Date:** 2026-06-30
**Working on:** Closing out the flash-lite cost experiment (t/1069) — Phase 2 + cite-only isolation test.
**Status:** COMPLETE. t/1069 closed Done — negative result. brief+cite=flash-lite regresses crux_addressed/utilization/claims_forgotten across both Phase 2 topics; cite-only isolation showed CITE is the culprit AND buys ~zero speed (the speedup is all in the brief stage). Decision: NO change to `stage_model_overrides` in calibration-config.json — keep brief+cite on premium model.
**Key context:** Full debate session lives at `debates/debate-<id>.json`; the cli-runs `<slug>-debate.json` is transient (gone after finalization) — get the id from `<slug>-harvest.json`, then join to calibration jsonl by debate_id. Comparison tools: `exp_compare.py` (2-way), `exp_citeonly_compare.py` (3-way). One labor-policy cite-only run died on a transient Opus-brief 120s API timeout (not a flash-lite fault) — cleared partial, reran clean via `exp-citeonly-labor.ps1`.
**Next:** Optional cleanup — gitignore the `exp-*` / `citeonly-logs/` scratch artifacts in research/comp-linguist. No open experiment work. Future: provide gate-trip/malformed-json thresholds for t/1167 when DebateTool ships the StageHarness (t/1166).
