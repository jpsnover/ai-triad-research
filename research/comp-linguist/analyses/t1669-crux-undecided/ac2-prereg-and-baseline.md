# t/1669 AC#2 — Pre-registration + pre-change baseline for the `crux_undecided` absorption measurement

**Author:** CL.Investigate1 (Computational Linguist)
**Date:** 2026-07-27
**Ticket:** t/1669 (AC#2, AC#4). Implementation: t/1676 (DebateTool, *in progress* — enum not yet landed).
**Status of this doc:** pre-registered BEFORE t/1676 lands the `undecided` verdict. Frozen so the
post-landing absorption pass is scored against a snapshot the tool's output cannot have contaminated.

Raw data: `ac2-baseline.json` (per-debate + frozen candidate frame), generated by `_baseline_scan.py`.
Both committed with this doc.

---

## Why now (time-sensitivity)

AC#2 asks: when the 4-value grammar is added, **what does `undecided` absorb, and is it calibrated
or a no-lose escape hatch?** (the paper's own caution, arXiv:2607.14399 F-2). Absorption is a
*before/after* comparison. The "before" — the crux-verdict distribution under the current 3-value
grammar — can only be captured while the grammar is still 3-value. Once t/1676 lands, the pre-change
snapshot is gone. This doc freezes it.

Methodologically, pre-registering the ground-truth interpretation *before* seeing the tool's
`undecided` assignments is also what keeps the CL judgment from being back-fitted to the tool
(the same discipline as PREREG-t1586 / PREREG-t1342).

---

## Pre-change baseline (frozen, 3-value grammar)

Scan of all archived debate sessions in `../ai-triad-data/debates/debate-*.json` (secondary
artifacts excluded):

- **244** debate session files; **135** carry cruxes; **1,754** cruxes total.
- Runtime `crux.state` distribution:

  | state | n | share |
  |---|---|---|
  | `identified` | **1,274** | **72.6%** |
  | `resolved` | 312 | 17.8% |
  | `one_side_conceded` | 100 | 5.7% |
  | `engaged` | 62 | 3.5% |
  | `irreducible` | 6 | 0.3% |

- **`resolution_status` is never persisted on `crux_tracker`** (distribution empty). The synthesis
  LLM grammar (`resolved`/`irreducible`/`active`, prompts.ts) is not written back onto cruxes; the
  runtime `state` is the only persisted crux verdict. → The state-based finalization sweep
  (t/1676, TL-confirmed at t/1676#4) is operating on the right signal; a synthesis-only derivation
  would have had nothing to read.

## The candidate pool and its calibration signal

The finalization sweep converts terminal-`identified` → `undecided`. That pool is **1,274 cruxes
(72.6%)** — so `undecided` will be the **modal** crux outcome. Two structural facts make that
defensible rather than alarming:

1. **It cannot cannibalize true terminal verdicts.** The sweep only touches `identified`; it never
   rewrites `resolved` (312), `irreducible` (6), `engaged` (62), or `one_side_conceded` (100).
   Precision against correct terminal verdicts is 100% by construction.

2. **`history` (state transitions) perfectly discriminates the pool**, so `identified` means
   "surfaced but never adjudicated" with zero exceptions:

   | state | n | history_len (min–max) | with history>0 |
   |---|---|---|---|
   | `identified` | 1,274 | 0–0 | **0** |
   | `engaged` | 62 | 1–1 | 62 |
   | `one_side_conceded` | 100 | 2–2 | 100 |
   | `resolved` | 312 | 2–3 | 312 |
   | `irreducible` | 6 | 2–2 | 6 |

   Every crux that was *ever engaged* left `identified` and recorded ≥1 transition. Every
   `identified`-terminal crux recorded **zero**. This is systematic (100%, not ~85%), i.e. the
   state machine faithfully records advancement and the `identified→undecided` rule maps exactly
   onto "never advanced."

   `speakers_involved ≥ 2` is **universal** on `identified` cruxes (1,274/1,274) and is therefore a
   **red herring** for calibration — a crux structurally names its opposing camps at creation; it
   is not evidence of cross-engagement. Do not treat multi-camp membership as a sufficiency signal;
   `history`/`state` is the signal.

## What is NOT yet established (residual AC#2 item)

The evidence above is drawn from the **state machine's own record**. It shows `undecided` (as the
sweep defines it) faithfully captures "never transitioned in the state machine." It does **not**
independently confirm that a zero-transition crux was genuinely un-adjudicated *in the transcript* —
in principle a crux could be argued out in prose while the state machine's `engaged` transition
under-fired. The 100% empty-history universality makes a systematic (conservative transition logic)
explanation far likelier than per-crux noise, but the honest final check is a transcript spot-check.

## Frozen golden set + pre-registered decision rules (post-landing pass)

**Sampling frame:** the 1,274 terminal-`identified` cruxes enumerated in
`ac2-baseline.json → identified_candidate_frame` (each `{file, crux_id, history_len, n_camps}`).
**Sample:** N = 30 cruxes, drawn deterministically (every 42nd = ⌊1274/30⌋-th entry of the frame in
file→crux_id sort order) so the sample is reproducible and fixed before any result is seen. Frozen
below (pre-landing, uncontaminated).

**Post-landing procedure:** after t/1676 lands, re-finalize (or re-run) and record each sampled
crux's assigned verdict. Then:

1. **Absorption:** confirm each newly-`undecided` crux was, in the pre-change baseline, in the
   "active"-miscounted pool (`identified`), NOT `resolved`/`irreducible`. (Expected 100% by the
   structural argument; measured, not assumed.)
2. **Calibration (transcript ground truth):** for each of the 30, CL reads the crux's transcript
   context and labels it genuinely-undecided (never cross-engaged / cap-terminated) vs
   engaged-but-unadvanced. Precision of the `identified→undecided` rule = fraction genuinely
   undecided.

**Pre-registered decision rules:**
- **Promote `crux_undecided_rate` stipulated→derived (AC#4)** only if precision ≥ **0.90** on the
  30-crux transcript read (≤3 engaged-but-unadvanced mislabels).
- **0.75 ≤ precision < 0.90:** verdict ships but stays **stipulated**; file a follow-up to tighten
  the sweep's eligibility (e.g. require the crux to have been re-raised across ≥2 turns before
  admitting `undecided`).
- **precision < 0.75:** the sweep is under-discriminating (a no-lose bucket); escalate to DebateTool
  to add a transcript-engagement gate before `undecided` is admitted, and do **not** promote.
- The `crux_addressed_rate` denominator change (t/1676) is a metric-semantics change: the cutover
  run-id/SHA must be recorded so pre/post values are never compared naively.

**Provenance:** `crux_undecided_rate` enters the register as **stipulated** when t/1676 lands
(t/1676 AC5, CL-coordinated); this pre-reg is the analysis pointer that will justify a later
stipulated→derived promotion iff the decision rule above is met.

---

## RESULTS (post-landing, 2026-07-27 — t/1676 landed `1228ad20`)

Landed implementation confirmed on origin/main: the finalization sweep (`cruxResolution.ts`)
transitions terminal-`identified` → literal `undecided`, gating **only** on `state === 'identified'`
(idempotent; `crux_addressed_rate` denominator now excludes `undecided`, explorationSummary.ts:348-350).
So the deterministic mapping this prereg assumed is exactly what shipped.

### Step 1 — absorption (decisive)
`undecided` uptake = the 1,274 terminal-`identified` cruxes = **72.6%** of all cruxes; drawn **100%**
from the pool previously miscounted as "active"; **zero** cannibalization of resolved/irreducible/
engaged/conceded (the sweep never touches them). Precision against *true terminal verdicts* is 100%
by construction. `undecided` is the modal crux outcome.

### Step 2 — calibration (the real test: is `undecided` == genuinely-not-adjudicated?)
CL adjudicated the frozen 30-crux sample from the content-anchored digests (`ac2-digests.txt`):
for each, did ≥2 opposing camps **adversarially engage the crux proposition** in prose (→ `undecided`
is a FALSE label) or was it only asserted/echoed/an evidentiary premise (→ genuinely un-adjudicated)?

- **~24/30 were adjudicated in prose** (FALSE `undecided`) — direct opposing rebuttals of the exact
  proposition. Unambiguous examples: AN-13/400f834d (skeptic: "mistakes the commodification of harm
  for the resolution of it"), AN-8/c4fe24f0 (skeptic rebuts "assumes plaintiffs survive a motion to
  dismiss"), AN-18/57c14a81 (acc: "I disagree that requiring… 20%… a stagnation levy"), AN-35, AN-51,
  AN-5, AN-44, AN-25, AN-9, AN-22×2, AN-1×2, AN-8/835bff57, AN-62, AN-26, AN-10, AN-15, AN-2, AN-14,
  AN-11×2, AN-66.
- **~6/30 genuinely un-adjudicated** (correct `undecided`) — evidentiary premises or asserted-and-
  echoed-without-contest: AN-29/1418 (Ford stat), AN-29/1ffff (Copyright-Office stat), D-1 (China-ban
  fact), AN-23 (Iceberg stat), AN-12, AN-24.

**Precision ≈ 6/30 ≈ 0.20** — far below the pre-registered 0.75 floor.

**Mechanism.** A crux *crystallizes* late: its `identified_turn` marks when cross-POV attackers cross
the detection threshold, but the underlying disagreement was argued *before/around* that point, and no
NEW argument-network edge lands *on the crux node* afterward — so `evaluateCruxState` never promotes it
past `identified`. Terminal-`identified` therefore proxies **"no post-crystallization edge on the crux
node,"** which is dominated by edge-extraction recall and crystallization timing — **NOT** genuine
non-adjudication. All 1,274 candidates having exactly 0 history transitions (baseline) is the same
artifact viewed structurally: the state machine simply does not re-touch these nodes after they surface.

**Method caveat.** Content-overlap + CL reading is a proxy for "proposition adversarially adjudicated";
it can over-count FALSE on same-topic-different-proposition turns. But the strongest cases are verbatim
opposing rebuttals of the exact crux text, so the direction (precision well below 0.75) is robust even
allowing for method error. DebateTool should confirm against the landed engagement logic.

### Decision (per pre-registered rules)
Precision < 0.75 → **do NOT promote `crux_undecided_rate` stipulated→derived** (stays stipulated with a
defect pointer), and **escalate a genuine engagement gate to DebateTool**: as shipped, the sweep will
report ~72.6% of cruxes "never adjudicated" when ~80% of a representative sample WERE adjudicated in
prose — a badly miscalibrated headline. Options for DebateTool (their design call): (a) gate `undecided`
on a transcript/AN-level engagement check rather than "no edge on the crux node"; (b) fix the crux
state machine's `identified→engaged` recall (edges/claims engaging the proposition, not only edges
whose endpoint is the crux node); (c) restrict `undecided` to cruxes surfaced within the last N turns
(cap-terminated), which is the sub-population where non-adjudication is genuine. Filed to t/1676 /
DebateTool.

**AC#2:** complete (absorption measured; calibration measured — fails the floor).
**AC#4:** NOT met — metric stays **stipulated**; no promotion.

## Resume trigger

