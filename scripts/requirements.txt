# AI Triad scripts — Python dependencies
# Install: pip install -r scripts/requirements.txt

# HTTP and web fetching
requests>=2.34.2
httpx>=0.28.1

# HTML to Markdown conversion
markdownify>=1.2.3
trafilatura>=2.1.0

# PDF text extraction
pypdf>=6.14.2
pdfminer.six>=20260107

# URL/slug utilities
python-slugify>=8.0.4

# Wayback Machine archival
waybackpy>=3.0.6

# AI API clients (install whichever you use)
anthropic>=0.120.2
# google-generativeai>=0.7

# Git inspection
gitpython>=3.1.57

# Semantic embeddings (all-MiniLM-L6-v2) and NLI cross-encoder
# (cross-encoder/nli-deberta-v3-small) for taxonomy search and
# contradiction detection. Models download on first use (~180MB each).
# Floor >=4.1 (t/1989): the CI reference vector was captured under 4.1.0 and
# verified BIT-IDENTICAL under 5.6.1 (cosine 1.0, maxabsdiff 0.0; probe run
# 30561385116) — so NO upper bound, 5.x is value-stable. The real contract is
# embedding VALUE stability vs the precomputed embeddings.json corpus, enforced by
# the `python-embed-smoke` CI job (cosine vs a pinned reference) + the weekly
# embed-drift workflow (which also catches transitive transformers/torch bumps
# this file's paths-filter can't see). An evidence-backed floor, not a break-pin.
sentence-transformers>=5.6.1

# Document conversion (PDF, DOCX, PPTX, XLSX, HTML, images → Markdown)
markitdown[all]>=0.1.6
