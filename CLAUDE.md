# AI Triad Research — Project Instructions

## What This Project Is

Multi-perspective research platform for AI policy & safety literature. Organizes sources through a four-POV taxonomy (accelerationist, safetyist, skeptic, cross-cutting), ingests documents, generates AI-powered summaries, and detects factual conflicts across viewpoints.

## Architecture

```
ai-triad-research/
├── scripts/
│   ├── AITriad/              # Main PowerShell 7 module
│   │   ├── AITriad.psm1      # Module root — dot-sources Private/ then Public/
│   │   ├── AITriad.psd1      # Manifest (v0.1.0)
│   │   ├── Public/           # Exported functions (21 commands)
│   │   └── Private/          # Internal helpers
│   ├── AIEnrich.psm1         # Multi-backend AI API abstraction (Gemini, Claude, Groq)
│   ├── DocConverters.psm1    # PDF/DOCX/HTML → Markdown conversion
│   └── PdfOptimizer.psm1     # PDF compression utilities
├── taxonomy/
│   ├── Origin/               # Authoritative POV JSON files + embeddings.json
│   └── schemas/              # JSON schemas for validation
├── sources/<doc-id>/         # Each source gets a directory
│   ├── metadata.json         # id, title, pov_tags, topic_tags, summary_status
│   ├── snapshot.md           # Markdown conversion of source document
│   └── raw/                  # Original PDF/HTML/etc.
├── summaries/<doc-id>.json   # AI-generated POV summaries with key_points + factual_claims
├── conflicts/                # Auto-detected factual conflicts across summaries
├── prompts/                  # AI prompt templates (analysis, taxonomy refinement)
├── taxonomy-editor/          # Electron + React + Vite desktop app
├── poviewer/                 # Electron + React + Vite — POV analysis viewer
├── summary-viewer/           # Electron + React + Vite — summary browser
└── docs/                     # Documentation and proposals
```

## Key Types

The `TaxonomyNode` class is defined in `AITriad.psm1` (not a separate file):
- Properties: POV, Id, Label, Description, Category, ParentId, Children, CrossCuttingRefs, Interpretations, LinkedNodes, Score

## Data Flow

1. **Ingest**: `Import-AITriadDocument` → creates `sources/<doc-id>/` with metadata.json + snapshot.md
2. **Summarize**: `Invoke-POVSummary` → reads snapshot, calls AI, writes `summaries/<doc-id>.json`
3. **Conflicts**: `Find-Conflict` → reads summary's factual_claims, creates/updates `conflicts/*.json`
4. **Health**: `Get-TaxonomyHealth` → analyzes coverage gaps across taxonomy + summaries

## Companion Modules

- `AIEnrich.psm1` — wraps Gemini, Claude, and Groq APIs behind a unified interface
- `DocConverters.psm1` — converts PDF, DOCX, HTML to Markdown for snapshot.md
- These are imported automatically by AITriad.psm1 at load time

## Taxonomy POVs

| POV             | Prefix | File                          |
|-----------------|--------|-------------------------------|
| Accelerationist | acc-   | taxonomy/Origin/accelerationist.json |
| Safetyist       | saf-   | taxonomy/Origin/safetyist.json       |
| Skeptic         | skp-   | taxonomy/Origin/skeptic.json         |
| Cross-cutting   | cc-    | taxonomy/Origin/cross-cutting.json   |

## Conventions

- PowerShell 7 required. Follow PS7 rules from global CLAUDE.md (Set-StrictMode, approved verbs, Try-Catch, pipeline pollution suppression).
- Module uses Public/Private split — new exported functions go in `Public/`, helpers in `Private/`.
- New public functions must be added to BOTH `AITriad.psm1` Export-ModuleMember AND `AITriad.psd1` FunctionsToExport.
- Electron apps all use the same stack: Electron + React + Vite. They share similar patterns for theming (Light/Dark/BKC/Auto).
- **AI prompts must be separate from code.** Never embed prompt template strings inline in hooks, stores, utilities, or components. In TypeScript apps, prompts go in `src/renderer/prompts/*.ts` as named export functions. In PowerShell, prompts go in `Prompts/*.prompt` and are loaded via `Get-Prompt`.
- Default AI model: `gemini-3.1-flash-lite-preview`. Configurable via `-Model` parameter or `$env:AI_MODEL`.
- Source doc IDs are slugs like `ai-as-normal-technology-2026`. Generated by `New-Slug` in Private/.
- `TAXONOMY_VERSION` file bump triggers CI batch re-summarization.
- **Node descriptions use genus-differentia format.** POV nodes: `"A [Category] within [POV] discourse that [differentia]. Encompasses: ... Excludes: ..."` CC nodes: `"A cross-cutting concept that [differentia]. Encompasses: ... Excludes: ..."` All prompts that create or modify descriptions enforce this pattern.
- **Debate agents use BDI-structured taxonomy context.** `formatTaxonomyContext` (in `taxonomy-editor/src/renderer/utils/taxonomyContext.ts`) groups POV nodes into Beliefs (Data/Facts), Values (Goals/Values), and Reasoning (Methods/Arguments) sections. Vulnerabilities and cross-cutting concerns are separate sections. Debate synthesis includes `bdi_layer` and `resolvability` fields (optional — absent in pre-migration debates).

## Environment Variables

- `GEMINI_API_KEY` — Google Gemini (primary backend)
- `ANTHROPIC_API_KEY` — Anthropic Claude
- `GROQ_API_KEY` — Groq
- `AI_API_KEY` — universal fallback
- `AI_MODEL` — override default model
