**Date:** 2026-07-20
**Working on:** t/1650 — `/api/nli/classify` must resolve + pass a BYOK/free-tier key to `classifyNli` (enables NLI hosted fallback post-venv-removal, t/1642).
**Status:** Done. Fix committed `ad581afc` (ancestor of origin/main `477aca0a`); verify gate green (exit 0) in fresh worktree off origin/main; venv-absent BYOK fallback path confirmed by inspecting committed `classifyNli`.
**Key context:** The code was already committed (`ad581afc`) and folded into origin/main by TL's reconcile before this session — this session was verification-and-close only. `/api/nli/classify` (server.ts ~L1312) now resolves `freeTierEmbeddingGate(req, res)`, early-returns on `gate.blocked`, passes `gate.key` to `ai.classifyNli(pairs, gate.key)`. Verify's earlier 7 `lib/debate/mcp-server.ts` TS2307 errors were a fresh-worktree root-dep gap (`@modelcontextprotocol/sdk` is a REPO-ROOT dep) — must `npm ci` at the worktree ROOT, not just `taxonomy-editor/`. Parent t/1641 transitions Done once this lands (Server AI Proxy p/165#12). t/1650 MUST precede t/1642 (venv removal).
**Next:** t/745 (index community submissions listing, low) in backlog. New session: read this, run `list_tickets(all:false, limit:500, sort:priority)`, pick highest-priority unblocked.
