**Date:** 2026-07-10
**Working on:** Log-line truncation + anonymous-session auth endpoint (post t/1473 auth-gate fix).
**Status:** All assigned tickets Done. Shipped: t/1461 (in-flight debate-save guard + slow-save FR log, 9adcfaa8), t/1473 (authOptional /api/* → JSON 401 not HTML, a9520212), t/1475 (Pino formatters.log truncation: 4KB field cap + 15KB line cap under ACA Fluent Bit, triage-field preservation, flight recorder keeps full fidelity — 1aa866ae), t/1483 (POST /api/auth/anonymous JSON sibling of GET redirect — cb0508ac). Filed t/1487 (web-bridge → POST migration, Taxonomy Editor, low, non-blocking).
**Key context:** `server.ts` boots the HTTP server on import → its inline pre-auth handlers (/.auth/anonymous, /api/auth/anonymous) can't be unit-tested by import; test the extracted pure helpers instead (`anonymousSessionCookies(randomId)` now lives in security/accessControl.ts next to `expiredAuthCookies`). Pre-auth handlers must sit BEFORE the auth gate (~line 2706). Pino truncation is Pino-output-only; flight-recorder events untouched. Known env flake: lib/ network tests (persistenceFaults, counterfactualCrux, lineDiff) fail on Gemini/ECONNRESET — not regressions; verify server suite in isolation.
**Next:** Queue clear (0 open). Watch for the t/1476 client-recovery land (TL was holding a DevOps deploy on it) and any t/1469 (Server Storage blob-timeout) follow-up needing the slow-save payload data from t/1461.
