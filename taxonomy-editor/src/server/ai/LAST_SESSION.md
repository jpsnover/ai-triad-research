**Date:** 2026-07-20
**Working on:** t/1641 — graceful API fallbacks for `classifyNli` + `updateNodeEmbeddings`/`computeQueryEmbedding`/`computeEmbeddings` in `aiBackends.ts`, so DevOps can strip the ~1GB Python ML venv from the ACA image (unblocks t/1642).
**Status:** DONE — t/1641 transitioned Done. All parts on origin/main: NLI fallback `41b4b338`; embedding ONNX-swap `39490e79` (also closes t/1643, folded via TL reconcile `f6e5f33d`); shared ONNX hardening t/1651 `61260804`; child t/1650 (ServerAPI key-pass for `/api/nli/classify`) `ad581afc`, Done. Full `npm run verify` green; ServerAPI re-verified in a fresh worktree off origin/main. DevOps pinged (p/208#4) — t/1642 gate released with the FLAT bake manifest.
**Key context:** Lossless Option B — in-process 384-dim ONNX all-MiniLM (`lib/embeddings/onnxEmbedding.ts`), NOT a Gemini migration; same embedding space, no downstream dimension change. Python stays primary when present; API/ONNX only on fallback. BYOK key rides the request, never baked/logged/in any FR record. `is429Error`/`isContextTooLongError` mutual-exclusivity untouched.
**Next:** Nothing on t/1641 — complete. New session: read ticket queue, pick next unblocked ticket. Do not chain.
