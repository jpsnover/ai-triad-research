**Date:** 2026-07-20
**Working on:** t/1641 embedding half + t/1643 fix — route hosted `updateNodeEmbeddings`/`computeQueryEmbedding`/`computeEmbeddings` to the shared 384-dim ONNX all-MiniLM-L6-v2 encoder (Python-primary-if-present) so DevOps can slim the ~1GB venv (unblocks t/1642).
**Status:** DONE. Shared Lib landed t/1651 (SHA `61260804`, on origin/main, in local HEAD). Edits applied to `aiBackends.ts`: aliased ONNX import; deleted dead `callGeminiBatchApi`/`callGeminiBatchEmbed`/`EMBED_TIMEOUT_MS`/`BATCH_SIZE`; `computeEmbeddings` python-batch primary + ONNX-batch fallback (kept `_explicitApiKey` param for server.ts arity); `computeQueryEmbedding` re-routed off the 3072-dim Gemini `RETRIEVAL_QUERY` fallback → shared 384-dim ONNX (**closes t/1643**); `updateNodeEmbeddings` unified `encodeBatch` closure (Python primary, ONNX fallback, ActionableError+FR if both fail) for both main + exclusion vectors. Full `npm run verify` PASSED (tsc main+server, eslint, depcruise, vitest, vite build). NLI half already landed `41b4b338`.
**Key context:** Shared ONNX API (`lib/embeddings/onnxEmbedding.ts`): `tryWarmup():Promise<boolean>` availability gate, `computeEmbedding(text)→384`, `computeEmbeddings(texts)→384[]` (imported aliased as `onnxComputeEmbeddings` — NAME COLLISION with local export). Same 384-dim/all-MiniLM space either way; no API key, no network. `is429Error`/`isContextTooLongError` mutual-exclusivity untouched.
**Next:** After commit — comment SHA on t/1641 + t/1643, transition t/1643 Done, ping DevOps the FLAT bake manifest (`model.onnx` fp32 + `tokenizer.json` + `tokenizer_config.json`, CPU EP) + the ONNX-swap SHA to release the t/1642 gate (NLI SHA `41b4b338` already sent). ServerAPI key-passing for `/api/nli/classify` hosted fallback = t/1650 (must land before t/1642). Git divergence reconcile is TL's, not mine.
