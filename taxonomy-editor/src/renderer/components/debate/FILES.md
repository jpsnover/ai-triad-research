# DebateUI — File Inventory

Reference inventory for the DebateUI scope (`components/debate/`). Behavioral norms — including the "Not in Scope — route, don't build" container-vs-workspace boundary — live in `AGENTS.md`.

## Key Files

| File | Purpose |
|------|---------|
| `DebateTab.tsx` | Main debate tab in the app sidebar |
| `DebatePopoutWindow.tsx` | Detached debate window |
| `DebateSourceViewer.tsx` | Source document viewer during debates |
| `NewDebateDialog.tsx` | Dialog for creating new debates |
| `CruxesTab.tsx` | Cruxes (key disagreements) tab |
| `SituationsTab.tsx` | Situations listing tab |
| `SituationDetail.tsx` | Detailed view of a single situation |
| `SituationDebatePanel.tsx` | Debate panel within situation context |
| `index.ts` | Barrel re-exports |
